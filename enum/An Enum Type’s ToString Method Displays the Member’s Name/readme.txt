Normally, when you call the ToString method on a variable that stores an integer value, the value of the integer is displayed.(see example 1)

If you call ToString on an enum type variable, however, the textual name of the enumerated member is displayed. (see example 2)